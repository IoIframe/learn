Firefox is having trouble recovering your windows and tabs. This is usually caused by a recently opened web page.

emoving one or more tabs that you think may be causing the problem

S

Well, this is embarrassing



git remote add origin git@github.com:IoIframe/learn.git   #create connection,you can see this in your account repository,which will show you what you need to write in your shell 

git push -u origin master     #push

git remote remove origin      #remove the connection

git clone git@github.com:(yourname)/(name of repository).git    #clone a repository to the localost,you need to build a repository first.



